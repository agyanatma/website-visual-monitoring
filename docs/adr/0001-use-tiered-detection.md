# Use tiered detection instead of AI for every check

We will classify each monitored URL with deterministic browser checks first, and only use lightweight AI visual review for suspicious or ambiguous cases. This keeps monitoring affordable and fast for hundreds of public URLs while still allowing visual judgment when simple signals are not enough.
